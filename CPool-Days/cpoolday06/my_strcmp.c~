/*
** EPITECH PROJECT, 2022
** My Strncmp
** File description:
** This function compares the two strings s1 and s2
*/

int my_strcmp(char const *s1, char const *s2)
{
    int final = 0;

    for (int i = 0; s1[i] != '\0' || s2[i] != '\0'; i++) {
        //if (s1 == s2) {
            //return (0);
            //}

        final = final + (s1[i] - s2[i]);
        printf("%d, %d, %d | ", final, s1[i], s2[i]);
    }

    return (final);
}
