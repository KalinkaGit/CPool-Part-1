/*
** EPITECH PROJECT, 2022
** My Macro Abs
** File description:
** A macro named ABS that replaces
** an argument with an absolute value.
*/

#ifndef BLOCK
    #define BLOCK
    #define ABS(value) (value < 0 ? -value : value)
#endif
